{
  "build": {
    "env": {
      "APP_ENV": "production",
      "APP_DEBUG": "true"
    }
  },
  "deploy": {
    "startCommand": "php artisan config:cache && php artisan route:cache && php artisan view:cache && php artisan serve --host=0.0.0.0 --port=8080",
    "restartPolicyType": "ON_FAILURE"
  }
}
